//Crie um script que declare uma variavel e verifique se o seu valor é booleano. Se for, exiba a mensagem "É boolean" senão "Não é boolean".

let myVariable = true

if(typeof myVariable === 'boolean') {
    alert("É boolean")
} else {
    alert("Não é boolean")
}